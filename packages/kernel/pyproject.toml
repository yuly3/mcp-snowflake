[project]
dependencies = [
    "attrs>=26.1.0",
    "cattrs-converter",
]
description = "domain layer components"
name = "kernel"
readme = "README.md"
requires-python = ">=3.13"
version = "0.1.0"

[build-system]
build-backend = "uv_build"
requires = [
    "uv_build>=0.11.28,<0.12.0",
]
